'use strict';

var Renderer = require('../lib/Renderer')

var should = require('should')

describe("Renderer", () => {
  var app;
  describe("Load", () => {
    it("should not be null", () => Renderer.should.not.be.null())

  });
});
